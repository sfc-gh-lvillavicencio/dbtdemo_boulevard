{{ config(materialized='table') }}

-- Inference example: score a single day using the registered model.
-- Returns prediction + 90% conformal prediction interval per row.
SELECT
    LOCATION_ID,
    SERVICE_CATEGORY,
    TARGET_DATE,
    PROVIDER_HOURS_DEMANDED AS ACTUAL,
    MODEL("BLVD_AI"."ML_DEMAND_MODEL"."LOCATION_SERVICE_DEMAND_XGBOOST", "LAST")!"PREDICT"(
        DAY_OF_WEEK,
        DAY_OF_MONTH,
        WEEK_OF_YEAR,
        DAY_OF_YEAR,
        IS_WEEKEND,
        IS_HOLIDAY,
        DAYS_TO_NEAREST_HOLIDAY,
        LOCATION_AGE_DAYS,
        LOCATION_STAFF_COUNT,
        COUNT_APPTS_L7D,
        COUNT_APPTS_L30D,
        COUNT_APPTS_DOW_AVG,
        COUNT_APPTS_LAG_7D,
        COUNT_APPTS_LAG_14D,
        COUNT_APPTS_LAG_28D,
        COUNT_APPTS_LAG_364D,
        COUNT_APPTS_TREND_4W,
        HOURS_PROVIDER_L7D,
        HOURS_PROVIDER_L30D,
        HOURS_PROVIDER_DOW_AVG,
        HOURS_PROVIDER_LAG_7D,
        HOURS_PROVIDER_LAG_14D,
        HOURS_PROVIDER_LAG_28D,
        HOURS_PROVIDER_LAG_364D,
        HOURS_PROVIDER_TREND_4W,
        HOURS_PROVIDER_PER_APPT_L30D,
        AVG_LEAD_TIME_DAYS,
        EMAIL_MSGS_L30D,
        SMS_MSGS_L7D,
        SF_ACCOUNT_TENURE,
        LOCATION_ID,
        SERVICE_CATEGORY,
        SF_SEGMENT,
        SF_INDUSTRY
    ) AS PREDICTION
FROM {{ ref('M1_1_RAW_TRAINING_DATA') }}
WHERE TARGET_DATE = '2026-04-08'
  AND PROVIDER_HOURS_DEMANDED IS NOT NULL